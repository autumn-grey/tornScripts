{
  "name": "torn-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Userscripts for Torn.com",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/tampermonkey": "^5.0.4",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
